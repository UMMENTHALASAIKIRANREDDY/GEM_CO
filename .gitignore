# Credentials — never commit these
.env
service_account.json

# Vault exports — contain sensitive user data
customers/*/vault_export/
customers/*/checkpoint.json
customers/*/migration_report.json
customers/*/visual_assets_report.json

# Uploads — contains vault exports and temp files
uploads/

# Node
node_modules/
